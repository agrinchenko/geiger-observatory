spring:
  datasource:
    url: jdbc:h2:mem:geiger-observatory;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  ingestion:
    interval-ms: 2000
    history-limit: 2000
  device:
    mode: simulator
